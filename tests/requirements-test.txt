# Test-only. Not installed on the server: keep the production set in
# ../requirements-push.txt free of anything the running service does not import.
# starlette's TestClient is built on httpx, so push_test.py needs it.
httpx==0.28.1

# The scrapers are parsed against real markup rather than a stub, because a
# stub would test nothing. Without these the parser checks skip and say so.

beautifulsoup4==4.13.4
lxml==5.3.0
